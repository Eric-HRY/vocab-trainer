# 啸啸单词斩 · 维护指南

> 写给啸啸：这个 App 的全部代码和数据都在这个文件夹里。日常「加单词」不需要会编程，
> 在 Kimi 里打开这个文件夹，直接对 Kimi 说人话就行（见下文「常见任务」）。

## 这是什么

- 一个背单词网页 App（PWA），已部署在 https://eric-hry.github.io/vocab-trainer/
- 技术栈：React 19 + TypeScript + Vite + Tailwind，记忆曲线（艾宾浩斯 1/2/4/7/15/30 天）
- 学习进度保存在**每台设备自己的浏览器里**（localStorage），不在网上，换设备不会同步

## 文件夹里有什么

| 位置 | 内容 | 要不要手动改 |
|---|---|---|
| `src/data/words.json` | 全部词条（544+）：单词、音标、中文释义、英文释义、例句 | 加词就改这里（一般让 Kimi 改） |
| `src/data/audio-manifest.json` | 每个词条 id 对应的音频文件名 | 脚本自动维护 |
| `public/audio/` | 全部单词发音文件（mp3/m4a） | 脚本自动生成 |
| `src/pages/` `src/lib/` | App 界面与记忆曲线逻辑 | 不懂就别动 |
| `scripts/` | 工具脚本：生成音频、冒烟测试、解析 PDF 词表 | 直接运行 |
| `dist/` | 构建产物 + 部署用的独立 git 仓库（gh-pages 分支） | 不用动，构建会自动生成 |

## 第一次在新电脑上准备

```bash
# 1. 装好 Node.js（https://nodejs.org，LTS 版）后：
git clone git@github.com:Eric-HRY/vocab-trainer.git
cd vocab-trainer
npm install
npm run dev        # 本地预览，浏览器打开 http://localhost:3000
```

## 常见任务（直接复制给 Kimi 说）

**加新单词**（比如老师发了新词表）：
> 把这份词表加进 src/data/words.json，按现有格式补齐音标、中文释义、英文释义和中英文例句，学科用已有的分类；id 从现有最大 id 往后编；然后运行 scripts 里的音频脚本给新词生成发音，构建并部署。

**改每日新词数量**：App 内设置页可直接调，不用改代码。

**改界面/功能**：
> 描述你想要的效果，改完运行 npm run build 和 npm run test:srs 确认无报错，再部署。

## 手动命令备查

```bash
npm run dev          # 本地预览
npm run test:srs     # 记忆曲线逻辑冒烟测试（改代码后必跑）
npm run build        # 构建到 dist/

# 部署上线（dist/ 是一个独立的 git 仓库，推 gh-pages 分支）：
cp dist/index.html dist/404.html
cd dist && git add -A && git commit -m "更新说明" && git push origin gh-pages
# 约 1 分钟后 https://eric-hry.github.io/vocab-trainer/ 生效
```

**音频脚本**（新词发音，自动：有道真人发音 → macOS say 兜底）：
```bash
python3 scripts/add-missing-audio.py        # 给清单里缺音频的词条补齐
python3 scripts/replace-spliced-audio.py    # 整句发音替换（可重跑，已完成的会跳过）
```

## 注意事项

- 词条 id 一旦发布**不要改**，学习进度按 id 记录，改 id 等于丢进度
- 删除词条同理：删了别人学过的词，他的对应进度会变成孤儿数据（不影响其他人）
- 每次改完先 `npm run build` + `npm run test:srs`，都过了再部署
- iPad/iPhone 上打开 App 会自动更新；没变化就把 App 彻底退出再打开
